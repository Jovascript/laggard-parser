def compile_grammar(grammar: str):
    """
    Compiles a grammar, given in string form, into a parser class.

    Args:
        grammar: The grammar

    Returns:
        A class descending from Parser
    """
